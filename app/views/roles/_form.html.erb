<%= simple_form_for [@project, @role] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :config %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
